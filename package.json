{
  "name": "listagem-usuarios-workspace",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm --prefix listagem_usuarios run dev",
    "build": "npm --prefix listagem_usuarios run build",
    "preview": "npm --prefix listagem_usuarios run preview"
  },
  "dependencies": {
    "solid-js": "^1.9.15"
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "vite-plugin-solid": "^2.11.14"
  }
}
